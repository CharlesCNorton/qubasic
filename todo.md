1. SPC(n) and TAB(n) in PRINT statements
2. PRINT separator behavior: semicolon suppresses newline, comma advances to next tab stop
3. Multiple PRINT zones for tabular output of measurement results
4. INPUT with type validation and retry (current INPUT silently defaults to 0 on error)
5. GET — single keypress input without enter (for interactive stepping)
6. REDIM — resize arrays
7. ERASE — delete a specific array
8. Random access file mode (complements existing sequential INPUT/OUTPUT/APPEND)
